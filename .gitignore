# dependencies
node_modules/
.pnp.*
.yarn/*

# next.js
.next/
out/
dist/
build/

# typescript
*.tsbuildinfo
next-env.d.ts

# runtime data
data/users/
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal
data/kb-index.sqlite*
data/auth.sqlite*
apps/web/data/*.sqlite
apps/web/data/*.sqlite-shm
apps/web/data/*.sqlite-wal

# generated study/audio artifacts
*.aiff
*.m4a

# unused generated visual experiment
apps/web/public/images/vitamap-background.png

# models cache (QMD downloads GGUFs here)
.cache/
~/.cache/qmd/

# env
.env
.env.local
.env*.local
!.env.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# misc
coverage/
.turbo/

# agent tooling (skills/agents instaladas + config local)
.claude/
.agents/
skills-lock.json

# material local, no del producto (guiones de audio y proyectos de video)
textToAudio/
videos/

# notas operativas propias, no publicables
local-notes/

# Tooling editorial externo (agente con el que se preparan las tarjetas).
# No forma parte de VitaMap ni de su despliegue: vive en su propio host y
# no comparte backup ni ciclo de vida con la aplicación.
corpus-preparation/hermes-skills/
corpus-preparation/hermes-skill-bundles/
corpus-preparation/PROMPT-HERMES-*.md

# =====================================================================
# Contenido del corpus RAG — fuera del repositorio público
# ---------------------------------------------------------------------
# El repositorio publica el MÉTODO (prompts, skills, scripts, taxonomía y
# el manual de creación de tarjetas), no el CONTENIDO. Las tarjetas viven
# en el disco local, en su ruta de siempre, para que el flujo de trabajo y
# los scripts sigan funcionando sin cambios; git simplemente no las sigue.
#
# Se conserva versionado el tema `vitamina-d/` como ejemplo normativo:
# cubre los cinco tipos de tarjeta (interpretación, alimentación,
# curiosidad, seguimiento temporal y fuente clásica).
corpus-preparation/approved-current-structure/*
!corpus-preparation/approved-current-structure/vitamina-d/
corpus-preparation/source-material/
corpus-preparation/previously-uploaded/
